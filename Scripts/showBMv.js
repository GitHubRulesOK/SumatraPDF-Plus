var d=mupdf.Document.openDocument(scriptArgs[0]);var tree=d.loadOutline();var it=d.outlineIterator();var stack=[{list:tree,index:0,level:0}];while(stack.length>0){var f=stack[stack.length-1];if(f.index>=f.list.length){stack.pop();if(stack.length>0){it.up();it.next();}continue;}var n=f.list[f.index];var ind=Array(f.level+1).join("  ");var mark=n.down?"v ":"  ";print(ind+mark+n.title);f.index++;if(n.down){it.prev();it.down();stack.push({list:n.down,index:0,level:f.level+1});}else{it.next();}}
